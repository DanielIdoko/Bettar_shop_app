import { ParamValue } from "next/dist/server/request/params";
import React from "react";

const Category = ({ params }: { params: ParamValue }) => {
  return <div>Category-</div>;
};

export default Category;
